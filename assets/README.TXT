EVERY TIME A NEW IDEA OR CHANGE IS NEEDED TO MAKE I WRITE A NOTE TO MAKE MY WORK EASIER SO THIIS JUST THAT THINK NOTHING ELSE

Drop these files here, named exactly as shown:

  Ronash_Sharma_Resume.pdf
  Ronash_Sharma_CV.pdf
  profile.jpg

The site's Resume/CV buttons and hero photo link directly to these filenames.
Whenever you update any of them, just overwrite the file here with the same
name, git push, and the live site updates automatically — no HTML/JS changes
needed.

Photo guidance:
- Portrait crop (4:5 ratio works best — e.g. 480x600px), it fills a real photo
  panel now, so resolution actually matters — keep it under ~500KB for fast loading
- Plain, professional headshot — same one you'd use on LinkedIn is fine
- If you don't add a photo, the site automatically falls back to showing your
  initials "RS" in a colored box instead of a broken image — nothing breaks
  either way, but a real photo is more standard for a portfolio.